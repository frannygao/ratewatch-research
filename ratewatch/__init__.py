"""RateWatch: Bank of Canada cut / hold / hike probabilities.

Release-date-aware features, a walk-forward backtest, and measurement that
separates the meetings where the Bank changed course from the ones it did not.

Run it with `python -m ratewatch`. Method: docs/method.md.
"""

__version__ = '2.0.0'
